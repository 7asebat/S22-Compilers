y: [4]float;
y[2] = 0.0;

my_procedure: proc(x: int, y: int) -> bool
{
    switch x + y
    {
        case 1, 2, 3 {
            x += y;
        }

        case 5, 6 {
            x += y;
        }

        case 4 {
            x += y;
        }
    }
    return true && 4 > 0;
}
my_procedure(1, 2);
